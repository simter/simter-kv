spring:
  datasource:
    continue-on-error: false
    sql-script-encoding: UTF-8
    initialization-mode: @db.init-mode@ # embedded|always|never
    # available when initialization-mode=embedded|always
    schema:
      # drop
      - classpath:tech/simter/kv/sql/h2/schema-drop.sql
      # create
      - classpath:tech/simter/kv/sql/h2/schema-create.sql
    data:
      - classpath:tech/simter/kv/sql/h2/data.sql
    driver-class-name: org.h2.Driver
simter:
  embedded-database:
    concat-sql-script: false # true - save concatenate sql content to file target/{platform}.sql